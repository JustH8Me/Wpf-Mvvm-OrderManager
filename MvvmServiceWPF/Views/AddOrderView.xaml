<UserControl x:Class="MvvmServiceWPF.Views.AddOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Создание заказа"
                   FontSize="24"
                   Margin="0,0,0,20" />

        <GroupBox Grid.Row="1" Header="Данные заказа" Padding="10">
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" /> 
                    <ColumnDefinition Width="350" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" /> 
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0"
                       Content="ФИО Клиента:"
                       VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding ClientName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5" Height="25" VerticalContentAlignment="Center" />
                
                <Label Grid.Row="1" Grid.Column="0"
                       Content="Описание работ:"
                       VerticalAlignment="Top" Margin="0,5,0,0" />
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5" Height="100"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" />
                <Label Grid.Row="2" Grid.Column="0"
                       Content="Номер телефона:" VerticalAlignment="Top" Margin="0,5,0,0" />
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding NumberTel, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5" Height="25" Width="150"
                         HorizontalAlignment="Left" VerticalContentAlignment="Center" />

                <Label Grid.Row="3" Grid.Column="0"
                       Content="Стоимость (руб):"
                       VerticalAlignment="Center" />
                <TextBox Grid.Row="3" Grid.Column="1"
                         Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,5" Height="25" Width="100"
                         HorizontalAlignment="Left" VerticalContentAlignment="Center" />
                
                <StackPanel Grid.Row="4" Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,15,0,0">
                    
                    <Button Content="Сохранить заказ"
                            Command="{Binding CreateCommand}"
                            Padding="15,5"
                            Height="30" />
                </StackPanel>

            </Grid>
        </GroupBox>

    </Grid>
</UserControl>