<UserControl x:Class="MvvmServiceWPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="160" />  
            <RowDefinition Height="*" />    
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="3" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>    
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Обзор показателей" 
                       FontSize="24" 
                       Foreground="#333333"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Период с:" VerticalAlignment="Center" Foreground="Gray"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="110" Margin="5,0,10,0" />
                
                <TextBlock Text="по:" VerticalAlignment="Center" Foreground="Gray"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="110" Margin="5,0,0,0" />
            </StackPanel>
        </Grid>
        
        <Border Grid.Row="1" Grid.Column="0" Background="#E8F5E9" CornerRadius="8" Margin="0,0,10,0" BorderBrush="#C8E6C9" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Всего заработано" FontSize="14" Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding TotalEarnings, StringFormat={}{0:N0} ₽}" FontSize="26" FontWeight="Bold" Foreground="#1B5E20" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
        
        <Border Grid.Row="1" Grid.Column="1" Background="#FFF3E0" CornerRadius="8" Margin="5,0,5,0" BorderBrush="#FFE0B2" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="В работе" FontSize="14" Foreground="#EF6C00" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding ActiveCount}" FontSize="32" FontWeight="Bold" Foreground="#E65100" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
        
        <Border Grid.Row="1" Grid.Column="2" Background="#E3F2FD" CornerRadius="8" Margin="10,0,0,0" BorderBrush="#BBDEFB" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Выполнено" FontSize="14" Foreground="#1565C0" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding CompletedCount}" FontSize="32" FontWeight="Bold" Foreground="#0D47A1" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

    </Grid>
</UserControl>