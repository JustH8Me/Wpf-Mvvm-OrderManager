<Window x:Class="MvvmServiceWPF.MainWindow"
        xmlns:vm="clr-namespace:MvvmServiceWPF.ViewModels"
        xmlns:views="clr-namespace:MvvmServiceWPF.Views"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Service Desk CRM" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ActiveOrdersViewModel}">
            <views:ActiveOrdersView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
            <views:HistoryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AddOrderViewModel}">
            <views:AddOrderView />
        </DataTemplate>
        
        <Style x:Key="MenuButtonStyle" TargetType="RadioButton">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="#ECF0F1"/> 
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                                Background="Transparent" 
                                Padding="15,0" 
                                CornerRadius="5">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#34495E"/> 
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2C3E50"/> 
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Background="#F5F7FA"> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/> 
            <ColumnDefinition Width="*"/>   
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Background="#222D32">
            <StackPanel Margin="10,20">
                
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="SERVICE DESK" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="CRM System" Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Center" FontStyle="Italic"/>
                </StackPanel>
                
                <RadioButton Content="📊  Дашборд" Command="{Binding ShowHomeCommand}" IsChecked="True" GroupName="Menu" Style="{StaticResource MenuButtonStyle}"/>
                
                <RadioButton Content="📋  Активные заказы" Command="{Binding ShowActiveCommand}" GroupName="Menu" Style="{StaticResource MenuButtonStyle}"/>
                
                <RadioButton Content="🕰  История" Command="{Binding ShowHistoryCommand}" GroupName="Menu" Style="{StaticResource MenuButtonStyle}"/>
                
                <RadioButton Content="➕  Новый заказ" Command="{Binding AddOrderCommand}" GroupName="Menu" Style="{StaticResource MenuButtonStyle}"/>
            </StackPanel>
        </Border>
        
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="0"/>
    </Grid>
</Window>